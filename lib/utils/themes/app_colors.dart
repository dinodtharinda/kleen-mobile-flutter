import 'package:flutter/material.dart';

class CustomColor{
 static const Color primaryColor = Color.fromARGB(255, 20, 112, 217);
 static const Color secondaryColor = Color(0xFFFFD06D);
 static const Color backColor = Colors.black;
 static const Color lightColor = Colors.white;
 static const Color darkColor = Colors.black;
 static const Color customGrey = Color.fromARGB(255, 212, 212, 212);

}